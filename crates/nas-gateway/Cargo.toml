[package]
name = "nas-gateway"
version = "0.1.0"
edition.workspace = true
license.workspace = true

[dependencies]
hmac = "0.12"
sha2 = "0.10"

[dev-dependencies]
# none — protocol tests talk to a real socket with std
